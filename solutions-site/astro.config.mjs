// @ts-check
import { defineConfig } from 'astro/config';

// Astro static site deployed to GitHub Pages via the official Actions pipeline.
export default defineConfig({
  site: 'https://kody-w.github.io',
  base: '/AI-Agent-Templates/',
});
